console.log('Ejercicios 1 - 4');
// Escribe una función que sume dos números.

function suma(a = 0, b = 0) {
    console.log(`Resta ${a} - ${b} = ${a + b}`);
}
suma(20, 5);

// Escribe una función que reste dos números.

function resta(a = 0, b = 0) {
    console.log(`Resta ${a} - ${b} = ${a - b}`);
}
resta(10, 5);

// Escribe una función que multiplique dos números.

function multi(a = 0, b = 0) {
    console.log(`Multiplicacion ${a} x ${b} = ${a * b}`)
}
multi(4, 4);


// Escribe una función que divida dos números.

function division(a = 0, b = 0) {
    console.log(`Division ${a} / ${b} = ${a / b}`)
}
division(9, 3);